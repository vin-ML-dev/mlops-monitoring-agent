apiVersion: v1
kind: Service
metadata:
  name: monitoring-agent-svc      # matches the architecture diagram
  namespace: finbot
  labels: { app: monitoring-agent }
spec:
  selector: { app: monitoring-agent }
  ports:
    - name: metrics
      port: 9108
      targetPort: 9108
  type: ClusterIP
