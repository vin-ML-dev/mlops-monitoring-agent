apiVersion: v1
kind: Service
metadata:
  name: fastapi-gateway-svc      # matches the architecture diagram
  namespace: finbot
spec:
  selector: { app: fastapi-gateway }
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
