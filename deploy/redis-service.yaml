apiVersion: v1
kind: Service
metadata:
  name: redis-svc          # the gateway addresses this name
  namespace: finbot
spec:
  selector: { app: redis }
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
