apiVersion: v1
kind: ConfigMap
metadata:
  name: gateway-config
  namespace: finbot
data:
  # non-secret runtime overrides (the app also reads configs/gateway.yaml)
  MODEL_BASE_URL: "http://llama-cpp-svc:8080"
  REDIS_URL: "redis://redis-svc:6379/0"
