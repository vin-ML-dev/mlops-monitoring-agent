# finbot — Day 1 (data) + Day 2 (fine-tuning) + Day 3 (eval gate) dependencies.

# ---- Core: Day 1 data pipeline (with the always-on regex PII layer) ----
pydantic>=2.6
langdetect>=1.0.9
PyYAML>=6.0
datasets>=2.19          # stream gbharti/finance-alpaca from Hugging Face
dvc>=3.50               # data + pipeline versioning

# ---- Day 2: QLoRA fine-tuning (needs a CUDA GPU) ----
transformers>=4.51      # >=4.51 required for Qwen3 support
peft>=0.13
trl>=0.12
bitsandbytes>=0.46.1    # 4-bit QLoRA loader; needs a CUDA GPU
accelerate>=1.1
sentencepiece>=0.2      # tokenizer support for conversion/inference
mlflow>=2.14
huggingface_hub>=0.26

# ---- Day 3: evaluation gate (CPU) ----
llama-cpp-python>=0.3   # loads the quantized GGUF to score it against the golden set

# ---- Optional NER PII layer (set pii.use_presidio: true; install via `make install-pii`) ----
# presidio-analyzer>=2.2
# presidio-anonymizer>=2.2
# spacy>=3.7            # then: python -m spacy download en_core_web_lg

# ---- Dev tooling ----
pytest>=8.0
ruff>=0.6
pre-commit>=3.7
